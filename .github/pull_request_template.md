## What does this PR do?

<!-- One-paragraph summary of the change. Link the related issue if one exists. -->

Closes #

## Type of change

- [ ] Bug fix
- [ ] New feature
- [ ] Refactor (no functional change)
- [ ] Docs / config only

## Checklist

- [ ] `pnpm test` passes
- [ ] `pnpm lint` passes
- [ ] No `any` types introduced
- [ ] New logic in `src/lib/` has tests in `tests/lib/`
- [ ] New API routes have tests in `tests/api/`
- [ ] Database schema changes use `prisma migrate dev`, not raw SQL
- [ ] No secrets or `.env` files committed

## Notes for reviewer

<!-- Anything the reviewer should pay special attention to. -->
